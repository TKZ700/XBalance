[project]
name = "xbalance"
version = "0.1.1-alpha"
description = "Multi-socket SOCKS5 load balancer for V2Ray/Xray configs"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "colorama>=0.4.6",
]

[project.scripts]
xbalance = "main:main_cli"
