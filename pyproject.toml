[project]
name = "xbalance"
version = "0.1.0"
description = "Multi-socket SOCKS5/HTTP load balancer for V2Ray/Xray configs with parallel downloads"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "colorama>=0.4.6",
    "rich",
]

[project.scripts]
xbalance = "main:main_cli"
